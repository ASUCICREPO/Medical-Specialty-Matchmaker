# AWS Account Configuration
# These values ensure the CDK stack deploys to the correct AWS account and region
# 
# Option 1: Set these as environment variables before running CDK commands:
#   export CDK_DEFAULT_ACCOUNT=123456789012
#   export CDK_DEFAULT_REGION=us-east-1
#
# Option 2: CDK will automatically use your AWS CLI profile's account and region
#   if these variables are not set
#
# Option 3: Copy this file to .env and source it:
#   cp .env.example .env
#   # Edit .env with your values
#   source .env  # On Linux/Mac
#   # Or set variables in your shell on Windows

# AWS Account ID (12-digit number)
CDK_DEFAULT_ACCOUNT=123456789012

# AWS Region (e.g., us-east-1, us-west-2, eu-west-1)
CDK_DEFAULT_REGION=us-east-1
