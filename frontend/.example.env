# AWS API Configuration
# IMPORTANT: API URLs use NEXT_PUBLIC_ prefix (exposed to browser)
# API Key uses AWS_ prefix (server-side only, NEVER exposed to browser)

# API Gateway URLs (safe to expose to browser)
NEXT_PUBLIC_API_URL=https://<PASTE-BASE-URL-AFTER-DEPLOYING-BACKEND-HERE>/prod/chatbot
NEXT_PUBLIC_DATA_URL=https://<PASTE-BASE-URL-AFTER-DEPLOYING-BACKEND-HERE>/prod/data

# API Key (server-side only - NEVER use NEXT_PUBLIC_ prefix for secrets!)
AWS_API_KEY=<PASTE-API-KEY-HERE>
